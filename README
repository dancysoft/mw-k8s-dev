# Install required packages

apt install make curl git jq

# Install minikube if needed. Requires sudo
make install-minikube

# Install kubectl if needed. Requires sudo
make install-kubectl

# Start minikube

make start-minikube

# Build and push images to minikube's registry
eval $(minikube docker-env)
time make images

# Use helm to install
make install
