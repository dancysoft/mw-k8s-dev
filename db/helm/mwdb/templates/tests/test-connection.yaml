apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mwdb.fullname" . }}-test-connection"
  labels:
{{ include "mwdb.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mwdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
